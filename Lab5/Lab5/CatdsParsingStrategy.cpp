#include "CatdsParsingStrategy.h"
#include <iostream>
#include <sstream>

using namespace std;

vector<string> CatdsParsingStrategy::parse(string s) {
	vector<string> result = { s, s };

	return result;
}
